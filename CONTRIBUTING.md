# Contributing

感谢你改进 Novel Style Distiller。

## 欢迎贡献

- 更准确的小说叙事、文风和语气提取规则；
- 能揭示误触发、剧情矛盾、POV 越界或文本复制的测试案例；
- 更清晰、可迁移的子 Skill 模板；
- 更可靠的运行时风格契约、场景模式和漂移检查；
- Story Bible、章节状态、线程、时间线和项目 writer 的改进；
- 人物/实体、关系、知识、连续性、摘要和修订记忆的改进；
- 能改变实际规划或诊断决策的长篇技法模块；
- 黄金三章、爽点/成长、钩子/留存、情绪波形和自然文风诊断的可验证改进；
- 批次写作、中断恢复、结构化变更和百章状态回归的改进；
- `novelctl.sh` 的可移植性、幂等性和路径安全改进；
- 合成小说或明确可再分发的公版测试材料；
- 中英文文档修正。

## 不要提交

- 未经授权的小说全文、节选、电子书或 OCR 文件；
- 以作者姓名作为写作指令的模仿 Prompt；
- 不能回指证据位置的风格断言；
- 把原作人物、世界观或情节逐点换名的示例；
- 在运行时风格包中保留来源身份、证据定位、引文或来源专名；
- 既没有单章验收也没有显式有界批次授权，却在草稿阶段修改 canon 的流程；
- 并行生成存在前后因果依赖的章节，或跳过逐章状态回写的“批量写”；
- 把所有技法或全部历史无条件塞入每章上下文的设计；
- 与小说蒸馏无关的运行时、自动化框架或平台包装。

## 修改要求

1. 保持 `SKILL.md` frontmatter 中的 `name` 和 `description` 准确、简洁。
2. 入口只保留共同流程；详细方法放入相关 `references/`。
3. 新增 extractor 时明确职责边界、输入、输出和自检项。
4. 新增产物时同时更新对应模板、`SKILL.md` 的输出结构和相关 reference；区分 `audit/`、`runtime-style-pack/` 与原创项目状态。
5. 检查所有相对链接有效，模板中没有意外遗留的真实小说内容。
6. 用至少三种叙事结构思考回归场景：第一人称或不可靠叙述、多人物限知、全知或非线性叙事。
7. 对 runtime pack 做来源隔离、跨题材、场景差异与至少三章漂移测试。
8. 对项目写作流程验证“草稿不回写、接受后回写、修订不误推进”的状态门。
9. 修改 `scripts/novelctl.sh` 时运行 `sh -n scripts/novelctl.sh`、`sh tests/smoke.sh` 与 `sh tests/longform-regression.sh`，并确认来源文件没有被复制进工作台。
10. 修改记忆结构时同步更新项目 writer、`doctor` 必需文件和 `references/11-long-form-memory-system.md`。
11. 提交前确认 `git diff --check` 通过，并在支持 Codex Skill 校验的环境中运行其标准快速校验。

## 测试材料

优先使用短小的合成小说。若使用公版作品，记录版本和来源，并确认其再分发条件允许提交。失败样本也要去除原作专名和大段原文。

单章状态回归样本应至少包含三章、一次明确接受、一次隐式接受和一次修订。批次/记忆变更需通过 120 章确定性合成回归，覆盖中断恢复、早期事实保留、知识变更、关系变更和线程兑现。测试产物必须使用完全合成的主题、人物与事件。
